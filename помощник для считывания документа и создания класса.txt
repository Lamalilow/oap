основная программа

using System;//дает возможность ссылаться на классы, которые находятся в пространстве имен System
using System.Collections.Generic;//дает возможность использовать колекции
using System.IO;//дает возможность работать с каталогами и файлами
//пустая строка
namespace ConsoleApp2 //имя программы
{//открывающая фигурная скобка
    class Program //класс программы
    {//открывающая фигурная скобка
        static void Main(string[] args) //метод программы Program
        {//открывающая фигурная скобка
            List<Animal> animals = new List<Animal>(); //создаем список animals, класса Animal
            string path = @"C:\Users\is42-ganeevfe\source\repos\ConsoleApp2\animal.txt"; //инициализируем путь  
            string line;//объявляем переменную line
            StreamReader sr= new StreamReader(path, System.Text.Encoding.UTF8);//объявляем класс, для считывания файла
            while ((line = sr.ReadLine()) != null) //пока файл не закончит считываться, будет происходить это
                //пустая строка
            {//открывающая фигурная скобка
                string[] words = line.Split(' ');//создаем массив данных и присваеваем ему значения списка, которые разделены пробелом
                if (words[0] == "лошадка")//если  первое слово лошадь, то...
                {//открывающая фигурная скобка
                    animals.Add(new Horse { Name = words[1], Food = words[2] });//в список добавляем элемент класса Horse, со значением второе слово Name, третье слово Food
                }//закрывающая фигурная скобка
                if (words[0] == "кошка")//если  первое слово кошка, то...
                {//открывающая фигурная скобка
                    animals.Add(new Cat { Name = words[1], Food = words[2] });//в список добавляем элемент класса Cat, со значением второе слово Name, третье слово Food
                }//закрывающая фигурная скобка
                if (words[0] == "собака")//если  первое слово собака, то...
                {//открывающая фигурная скобка
                    animals.Add(new Dog { Name = words[1], Food = words[2] });//в список добавляем элемент класса Dog, со значением второе слово Name, третье слово Food
                }//закрывающая фигурная скобка
                //пустая строка
                //пустая строка
            }//закрывающая фигурная скобка
            foreach (var animal in animals)//цикл для перебора животных(animal) в списке animals
            {//открывающая фигурная скобка
                animal.Info();//вызов метод Info
            }//закрывающая фигурная скобка
            
            //1. Какой класс является родителем классов cat, dog, horse?
            //2. В программе определён список, какого типа элементы этого списка?
            //3. Почему мы можем добавлять
            //4. 
            Console.Read();//метод, используемый для ввода текста(чтобы консольное приложение не закрылось)
        }//закрывающая фигурная скобка
    }//закрывающая фигурная скобка
}//закрывающая фигурная скобка


класс Animal
using System;//дает возможность ссылаться на классы, которые находятся в пространстве имен System
using System.Collections.Generic;//дает возможность использовать колекции
using System.IO;//дает возможность работать с каталогами и файлами
                //пустая строка
namespace ConsoleApp2 //имя программы
{//открывающая фигурная скобка
    class Animal //класс программы
    {//открывающая фигурная скобка
        public string Name; //поле класса
        public string Food;//поле класса
        public virtual void Info()//метод класса
        {//открывающая фигурная скобка
            Console.WriteLine("Животное " + Name + " любит " + Food);//метод для вывода информации
        }//закрывающая фигурная скобка
    }//закрывающая фигурная скобка
}//закрывающая фигурная скобка


дальше наследуемые классы
класс Cat
using System;//дает возможность ссылаться на классы, которые находятся в пространстве имен System
using System.Collections.Generic;//дает возможность использовать колекции
using System.IO;//дает возможность работать с каталогами и файлами
                //пустая строка
namespace ConsoleApp2//имя программы
{//открывающая фигурная скобка
    class Cat: Animal//класс программы наследуемый Animal
    {//открывающая фигурная скобка
        public override void Info()
        {//открывающая фигурная скобка
            Console.WriteLine("Кошечка " + Name + " любит " + Food);//метод для вывода информации
        }//закрывающая фигурная скобка
    }//закрывающая фигурная скобка
}//закрывающая фигурная скобка

класс Dog
using System;//дает возможность ссылаться на классы, которые находятся в пространстве имен System
using System.Collections.Generic;//дает возможность использовать колекции
using System.IO;//дает возможность работать с каталогами и файлами
                //пустая строка
namespace ConsoleApp2//имя программы
{//открывающая фигурная скобка
    class Dog: Animal//класс программы наследуемый Animal
    {//открывающая фигурная скобка
        public override void Info()
        {//открывающая фигурная скобка
            Console.WriteLine("Собачка " + Name + " любит " + Food);//метод для вывода информации
        }//закрывающая фигурная скобка
    }//закрывающая фигурная скобка
}//закрывающая фигурная скобка

класс Horse
using System;//дает возможность ссылаться на классы, которые находятся в пространстве имен System
using System.Collections.Generic;//дает возможность использовать колекции
using System.IO;//дает возможность работать с каталогами и файлами
                //пустая строка
namespace ConsoleApp2//имя программы
{//открывающая фигурная скобка
    class Horse: Animal//класс программы наследуемый Animal
    {//открывающая фигурная скобка
        public override void Info()
        {//открывающая фигурная скобка
            Console.WriteLine("Лошадка " + Name + " любит " + Food);//метод для вывода информации
        }//закрывающая фигурная скобка
    }//закрывающая фигурная скобка
}//закрывающая фигурная скобка
